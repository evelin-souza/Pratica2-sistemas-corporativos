# Pratica2-sistemas-corporativos

```bash
# Construir e iniciar os containers
docker compose up --build -d

# Visualizar logs
docker compose logs -f

# Encerrar e remover containers
docker compose down
```
